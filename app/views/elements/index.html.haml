%h1 Your Elements

%h3
  View:
  - if params[:view] == 'ranked'
    = link_to('Tree', elements_path)
    | Ranked
  - else
    Tree |
    = link_to('Ranked', elements_path(:view => 'ranked'))

%h3= link_to('Add new Element', new_element_path)
%ul
  = render @elements
